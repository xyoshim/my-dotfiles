#

## Set the prefix to ^A.
unbind C-b
set -g prefix ^A
bind a send-prefix

## Set term 256 color
set -g default-terminal 'xterm-256color'
set -g terminal-overrides 'xterm:colors=256'

## Reload ~/.tmux.conf
unbind R
bind R source-file ~/.tmux.conf \; display "Configuration Reloaded!"

## Status-bar
set-option -g status-right "#[fg-black][%Y/%m/%d %H:%M:%S]"
# set-option -g status-right "#[%Y/%m/%d %H:%M:%S]"
set-option -g status on
set-option -g status-position bottom
# set status-interval 15

#unbind-key C-b
#set-option -g prefix C-j
#bint C-j send-prefix
bind C-r source-file ~/.tmux.conf
# set -g set-titles off
#set-window-option -g utf8 on
set-window-option -g mode-keys vi
#set-window-option -g automatic-rename off
# set-window-option -g automatic-rename on
#set-option -g status-left '#[fg=cyan,bold][#S]#[default]'
#set-option -g status-left "[#I]"
bind C-u set status-left ""
#set-option -g set-titles on
#set-option -g set-titles-string "#(whoami)@#H: tmux"
bind C copy-mode
bind P paste-buffer -s \015
bind ^[ copy-mode
bind ^] paste-buffer -s \015
# set -g window-status-format "#I:#(exec basename $SHELL)"
# set -g window-status-current-format "#I:#(exec basename $SHELL)*"
#set -g window-status-current-format "#I:#(exec basename `pwd`)*"
#set -g window-status-format "#I:#(exec basename `pwd`)"
#bind C-y set status-right "#(/bin/nice -n 20 awk '{print $1,$2,$3}'</proc/loadavg)  %T"
# bind C-y set status-right "#(exec cut -d ' ' -f 1,2,3</proc/loadavg)  %T"
# bind C-t set status-right "%T"
#bind C-y set status-right "#(exec nice -n 20 cut -d ' ' -f 1,2,3</proc/loadavg)  %T"
#set status-right "#(cut -d ' ' -f 1,2,3 /proc/loadavg) #(date '+%T')"
# set status-right "#(date '+%T')"
#set status-right "#(/bin/nice -n 20 awk '{print $1,$2,$3}'</proc/loadavg)  %T"
#set status-right "#(uptime|awk '{print $8 $9 $10}')  %T"
#set status-interval 1
# set status-interval 15
##color
#set-option -g status-bg blue
#set-option -g status-fg yellow
#set-option -g history-limit 100000

## $Id: screen-keys.conf,v 1.7 2010/07/31 11:39:13 nicm Exp $
##
## By Nicholas Marriott. Public domain.
#
## This configuration file binds many of the common GNU screen key bindings to
## appropriate tmux key bindings. Note that for some key bindings there is no
## tmux analogue and also that this set omits binding some commands available in
## tmux but not in screen.
##
## Note this is only a selection of key bindings and they are in addition to the
## normal tmux key bindings. This is intended as an example not as to be used
## as-is.

## Bind appropriate commands similar to screen.
## lockscreen ^X x
unbind ^X
bind ^X lock-server
unbind x
bind x lock-server

## screen ^C c
unbind ^C
#bind ^C new-window 'tmux set window-name $SHELL'
bind ^C new-window
unbind c
#bind c new-window "tmux set window-name $SHELL"
bind c new-window

## detach ^D d
unbind ^D
bind ^D detach

## displays *
unbind *
bind * list-clients

## next ^@ ^N sp n
unbind ^@
bind ^@ next-window
unbind ^N
bind ^N next-window
unbind " "
bind " " next-window
unbind n
bind n next-window

## title A
unbind A
bind A command-prompt "rename-window %%"

## other ^A
unbind ^A
bind ^A last-window

## prev ^H ^P p ^?
unbind ^H
bind ^H previous-window
unbind ^P
bind ^P previous-window
unbind p
bind p previous-window
unbind BSpace
bind BSpace previous-window

## windows ^W w
unbind ^W
bind ^W list-windows
unbind w
bind w list-windows

## quit \
unbind '\'
bind '\' confirm-before "kill-server"
### \'' ###

## kill K k
unbind K
bind K confirm-before "kill-window"
unbind k
bind k confirm-before "kill-window"

## redisplay ^L l
unbind ^L
bind ^L refresh-client
unbind l
bind l refresh-client

## split -v |
unbind |
bind | split-window

## :kB: focus up
unbind Tab
bind Tab select-pane -t:.+
unbind BTab
bind BTab select-pane -t:.-

## " windowlist -b
unbind '"'
bind '"' choose-window

## mouse
setw -g mouse on
#set -g mouse on
unbind -n MouseDrag1Pane
unbind -Tcopy-mode MouseDrag1Pane
# set -g mouse off
# bind-key -T edit-mode-vi WheelUpPane send-keys -X scroll-up
# bind-key -T edit-mode-vi WheelDownPane send-keys -X scroll-down

## for clipboard
if-shell "test -w /dev/clipboard" "bind-key > save-buffer /dev/clipboard"
if-shell "test -w /dev/clipboard" "bind-key < load-buffer /dev/clipboard"

##set-window-option -g mode-keys vi
#bind-key -t vi-copy v begin-selection
#bind-key -t vi-copy C-v rectangle-toggle
#bind-key -t vi-copy y copy-pipe "xsel -bi"
#bind-key -t vi-copy Enter copy-pipe "xsel -bi"
